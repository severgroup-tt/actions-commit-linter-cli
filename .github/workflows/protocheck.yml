# Terraform autogenerated!

name: Protocheck
on: [push, pull_request]
jobs:
  protocheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: severgroup-tt/protocheck@master

